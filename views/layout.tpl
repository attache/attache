<!DOCTYPE html>
<html>
    <head>
        <title>
            {{block "title" .}}Welcome{{end}}
        </title>
        <!-- styles -->
        {{block "styles" .}}{{end}}
        <!-- scripts -->
        {{block "scripts" .}}{{end}}
    </head>
    <body>
        {{block "body" .}}{{end}}
    </body>
</html>